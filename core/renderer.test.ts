import { describe, expect, it } from 'vitest'

import { render } from '../core/renderer'
import { emptyDocument } from '../core/types'

describe('heading', () => {
  it('noops', () => {
    expect(true).toMatchInlineSnapshot('false')
  })
})
